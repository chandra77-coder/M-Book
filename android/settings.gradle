pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

// Repositories are defined in build.gradle for better compatibility with different Gradle versions

rootProject.name = "Mbook"
include ':app'
